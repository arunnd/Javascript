//unary negation was applied
let x = 1;

x = -x;
alert( x ); // -1, unary negation was applied

//////////////////////////

//binary minus subtracts values
let x = 1, y = 3;
alert( y - x ); // 2, binary minus subtracts values


//String concatenation
alert( '1' + 2 ); // "12"
alert( 2 + '1' ); // "21"

alert( 2 - '1' ); 
alert( '6' / '2' ); 

//unary plus
let apples = "2";
let oranges = "3";

alert( +apples + +oranges ); // 5